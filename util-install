# install direnv
sudo apt install -y direnv
sudo apt-get install -y emacs25-nox git
sudo apt install linuxbrew-wrapper


# install kubectl

#curl -LO https://storage.googleapis.com/kubernetes-release/release/$(curl -s https://storage.googl#eapis.com/kubernetes-release/release/stable.txt)/bin/linux/amd64/kubectl
#chmod +x ./kubectl
#sudo mv ./kubectl /usr/local/bin/kubectl

# install keychain
pushd .
cd ~
git clone git@github.com:funtoo/keychain.git
popd

# install emacs stuff
pushd .
cd ~
git clone git@github.com:gherlein/elisp.git
cd elisp
./install.sh
popd

